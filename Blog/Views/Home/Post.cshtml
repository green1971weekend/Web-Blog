@model Post

@{ 

}


<h2>@Model.Title</h2>

@if(!string.IsNullOrEmpty(Model.Image))
{
    var img_path = $"/Image/{Model.Image}";
    <img src="@img_path" alt="Downloaded image" width="500" />
}

<p>@Model.Body</p>